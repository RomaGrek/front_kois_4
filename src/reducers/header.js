const initialState = {
    secondName: "Golovkov Michail &",
    firstName: "Loginov Roman",
    variant: "dorohidoro",
    topic: "Сourse project"
};

export function headerReducer(state = initialState) {
    return state;
}